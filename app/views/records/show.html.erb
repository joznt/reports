<p id="notice"><%= notice %></p>

<p>
  <strong>Data:</strong>
  <%= @record.data %>
</p>

<p>
  <strong>Total:</strong>
  <%= @record.total_km %>
</p>

<%= link_to 'Edit', edit_record_path(@record) %> |
<%= link_to 'Back', records_path %>
